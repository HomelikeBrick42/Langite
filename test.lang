struct True {}
struct False {}
enum Bool {
    true: True,
    false: False,
}

struct Foo {}
struct Bar {}

fn foo(T: Type) -> Type {
    match T {
        Foo => Bar,
        _ => T,
    }
}

fn bar(const T: Type, x: T) -> foo(T) {
    x
}
